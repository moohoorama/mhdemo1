# AGENTS.md

Read `README.md` before modifying this repository.

In particular, follow the asset pipeline documented there. Do not add
project-specific sprite transformations to `tools/spritetool`. Keep runtime
assets under `assets/` and source artwork under `tools/spritetool/assets/`.
